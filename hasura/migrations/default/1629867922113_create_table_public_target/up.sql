CREATE TABLE "public"."target" ("id" UUID NOT NULL DEFAULT gen_random_uuid(), "picture" varchar(2083) NOT NULL, "course_id" UUID NOT NULL DEFAULT gen_random_uuid(), "title" varchar(20) NOT NULL, "description" varchar(5000) NOT NULL, "sort_order" smallserial NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("course_id") REFERENCES "public"."courses"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("sort_order"));
